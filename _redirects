# Cloudflare Pages: repo-only files are deployed with the site (no build step), so send them home instead of serving them.
# GitHub Pages ignores this file; there, _config.yml's `exclude` (plus Jekyll's dot/underscore rule) keeps them out.
/CLAUDE.md      /  301
/_config.yml    /  301
/.gitignore     /  301
/.claude/*      /  301
/tools/*        /  301
