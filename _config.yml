# GitHub Pages (Jekyll) only: keep repo tooling and agent notes out of the published site.
# Dot- and underscore-prefixed paths (.claude/, .gitignore, _headers, _redirects) are already excluded by Jekyll.
# Cloudflare Pages ignores this file; there, _redirects does the same job.
exclude:
  - CLAUDE.md
  - tools/
